# ---- Dependencies ----
node_modules/
.local-tools/
.pnp
.pnp.js

# ---- Environment / secrets (never commit real keys) ----
.env
.env.local
.env.*.local
frontend/.env
backend-node/.env
*token.json*
*credentials.json*

# ---- Production builds ----
/build
frontend/build
dist/
dist

# ---- Testing / coverage ----
/coverage

# ---- Logs ----
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# ---- OS / editor ----
.DS_Store
.idea/
.vscode/

# ---- Emergent platform (local only, not part of the app) ----
.emergent/
memory/

# ---- Google Cloud service-account key (never commit) ----
gcp-service-account.json
**/gcp-service-account.json
.env.*
*.env
credentials.json
*.pem
*.key
.credentials
.git-credentials
.gitconfig
